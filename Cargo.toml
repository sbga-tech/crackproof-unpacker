[package]
name = "crackproof-unpacker"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
aes = "0.8"
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
libmwemu = "0.25.3"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
